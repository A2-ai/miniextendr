# Rust-based, R-package `rpkg`

## Developer Guide

The `/configure` script ought to be updated during development time, this is
done using

```shell
autoreconf
```

Users need not run this.

## TODO

- [ ] Update `.Rbuildignore` in `rpkg`.
- [ ] Rename the embedded library to something else other than `juice`.
