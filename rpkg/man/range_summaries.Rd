% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/range_summaries.R, R/miniextendr-wrappers.R
\name{range_summaries}
\alias{range_summaries}
\alias{box_width}
\alias{box_covers}
\alias{box_width.RangeBox}
\alias{box_covers.RangeBox}
\alias{rangebox_from_values}
\alias{range_width}
\alias{range_within}
\alias{range_clamp}
\alias{range_midpoint}
\alias{box_midpoint.RangeBox}
\alias{box_clamp.RangeBox}
\title{Range summaries}
\usage{
\method{box_width}{RangeBox}(x, ...)

\method{box_covers}{RangeBox}(x, values, ...)

rangebox_from_values(values)

range_width(values)

range_within(values, lower, upper, inclusive)

range_clamp(values, lower, upper, outside = c("clamp", "drop"))

range_midpoint(values)

\method{box_midpoint}{RangeBox}(x, ...)

\method{box_clamp}{RangeBox}(x, values, ...)
}
\arguments{
\item{x}{A \code{RangeBox} object.}

\item{...}{Unused; accepted for S3 method compatibility.}

\item{values}{A numeric vector.}

\item{lower, upper}{Lower and upper bound of the range.}

\item{inclusive}{Whether a value equal to a bound counts as inside.}

\item{outside}{What to do with values outside the range: \code{"clamp"} moves
them onto the nearer bound, \code{"drop"} removes them.}
}
\value{
\code{range_width()}, \code{range_midpoint()}, \code{box_width()} and
\code{box_midpoint()} return a number, \code{range_within()} and \code{box_covers()} a
logical vector with one element per value, \code{range_clamp()} and
\code{box_clamp()} a numeric vector, and \code{rangebox_from_values()} a
\code{RangeBox}.
}
\description{
Small summaries of where numeric values sit relative to a range. The
functions share this page, and each argument is described once for all
of them.

Which values a range box covers, bounds included.

Width of the values' range: the largest value minus the smallest.

Which values lie inside the range.

Keep the values inside the range, handling the others as \code{outside} says.
}
\section{Functions}{
\itemize{
\item \code{box_width(RangeBox)}: Width of a range box.

\item \code{rangebox_from_values()}: The smallest range box holding every
value.

\item \code{range_midpoint()}: Midpoint between the smallest and the largest
value.

\item \code{box_midpoint(RangeBox)}: Midpoint of a range box, halfway between
its bounds.

}}
\examples{
x <- c(2, 5, 9)
range_width(x)
range_within(x, 3, 9, inclusive = TRUE)
range_clamp(x, 3, 8)
range_clamp(x, 3, 8, outside = "drop")
range_midpoint(x)
box <- rangebox_from_values(x)
box_width(box)
box_covers(box, c(1, 5))
}
