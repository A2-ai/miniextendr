% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miniextendr-wrappers.R
\name{native_sexp_altrep_new}
\alias{native_sexp_altrep_new}
\title{Create a native-SEXP ALTREP integer vector backed by a plain `INTSXP`}
\source{
Generated by miniextendr from Rust fn `native_sexp_altrep_new`
}
\usage{
native_sexp_altrep_new(values)
}
\arguments{
\item{values}{An integer vector.}
}
\value{
An ALTREP-backed integer vector.
}
\description{
Unlike the usual ExternalPtr-backed ALTREP, the backing data (`data1`) is a plain R integer vector stored directly in the ALTREP `data1` slot. `AltrepExtract` is implemented via a static singleton: the ZST `NativeSexpIntAltrep` has no state — all data lives in `data1` on the ALTREP SEXP itself.
}
\details{
This mirrors R's own `compact_intseq` pattern (see `src/main/altclasses.c` in the R source), where `data1` stores the sequence parameters and all element-access methods derive their results from `data1` directly.
}
