% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miniextendr-wrappers.R
\name{EitherRouteS7}
\alias{EitherRouteS7}
\alias{EitherRouteS7-route_given}
\alias{EitherRouteS7$route_given}
\alias{EitherRouteS7_route_given}
\alias{EitherRouteS7-plan_s7}
\alias{EitherRouteS7$plan_s7}
\alias{EitherRouteS7_plan_s7}
\alias{s7_trait_EitherGrade_either_grade}
\alias{EitherRouteS7_either_grade}
\title{EitherRouteS7 S7 Class}
\usage{
EitherRouteS7(start = c("oral", "bolus", "infusion"), .ptr = NULL)

EitherRouteS7_route_given(self, ...)

EitherRouteS7_plan_s7(
  self,
  route = c("oral", "bolus", "infusion"),
  level = c("low", "mid", "high"),
  ...
)

EitherRouteS7_either_grade(self, grade = c("low", "mid", "high"), ...)
}
\arguments{
\item{start}{One of "oral", "bolus", "infusion", or a data frame; omitting the argument means no choice.}

\item{.ptr}{Internal pointer (used by static methods, not for direct use).}

\item{self}{A \code{EitherRouteS7} object.}

\item{...}{Additional arguments; ignored by the fast-path shortcut.}

\item{route}{One of "oral", "bolus", "infusion", or a data frame.}

\item{level}{One of "low", "mid", "high", or a number; omitting the argument means no choice.}

\item{grade}{One of "low", "mid", "high", or a number; omitting the argument means no choice.}
}
\description{
What the constructor was given.

Calls the underlying Rust routine directly, bypassing \code{S7::S7_dispatch()}
(the class walk + method-table lookup). Use in hot loops where the
per-call dispatch overhead matters. \strong{Footgun:} this shortcut does not
perform subclass dispatch — a method override defined on a child class
will \emph{not} be honoured. Use the generic when subclassing is possible.

A route name or a data frame of doses, and an omittable level name or number.

Calls the underlying Rust routine directly, bypassing \code{S7::S7_dispatch()}
(the class walk + method-table lookup). Use in hot loops where the
per-call dispatch overhead matters. \strong{Footgun:} this shortcut does not
perform subclass dispatch — a method override defined on a child class
will \emph{not} be honoured. Use the generic when subclassing is possible.

S7 generic for trait method \code{EitherGrade::either_grade}

Calls the underlying Rust routine directly, bypassing \code{S7::S7_dispatch()}
(the class walk + method-table lookup). Use in hot loops where the
per-call dispatch overhead matters. \strong{Footgun:} this shortcut does not
perform subclass dispatch — a method override defined on a child class
will \emph{not} be honoured. Use the generic when subclassing is possible.
}
