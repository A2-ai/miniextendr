% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miniextendr-wrappers.R
\name{deferred_warning_value}
\alias{deferred_warning_value}
\alias{deferred_mixed_order}
\alias{deferred_message_struct}
\alias{deferred_condition_value}
\alias{deferred_condition_typed}
\alias{deferred_then_error}
\alias{deferred_then_panic}
\alias{deferred_then_warning_abort}
\alias{deferred_unit}
\alias{deferred_rng_value}
\alias{deferred_nested}
\alias{deferred_worker_value}
\alias{deferred_guard_altrep}
\alias{deferred_guard_nested}
\alias{deferred_pending_count}
\alias{deferred_finalizer_pointer}
\alias{deferred_finalized_count}
\alias{deferred_guard_connection}
\title{deferred_warning_value}
\source{
Generated by miniextendr from Rust fn \code{deferred_warning_value}

Generated by miniextendr from Rust fn \code{deferred_mixed_order}

Generated by miniextendr from Rust fn \code{deferred_message_struct}

Generated by miniextendr from Rust fn \code{deferred_condition_value}

Generated by miniextendr from Rust fn \code{deferred_condition_typed}

Generated by miniextendr from Rust fn \code{deferred_then_error}

Generated by miniextendr from Rust fn \code{deferred_then_panic}

Generated by miniextendr from Rust fn \code{deferred_then_warning_abort}

Generated by miniextendr from Rust fn \code{deferred_unit}

Generated by miniextendr from Rust fn \code{deferred_rng_value}

Generated by miniextendr from Rust fn \code{deferred_nested}

Generated by miniextendr from Rust fn \code{deferred_worker_value}

Generated by miniextendr from Rust fn \code{deferred_guard_altrep}

Generated by miniextendr from Rust fn \code{deferred_guard_nested}

Generated by miniextendr from Rust fn \code{deferred_pending_count}

Generated by miniextendr from Rust fn \code{deferred_finalizer_pointer}

Generated by miniextendr from Rust fn \code{deferred_finalized_count}

Generated by miniextendr from Rust fn \code{deferred_guard_connection}
}
\usage{
deferred_warning_value(n)

deferred_mixed_order()

deferred_message_struct()

deferred_condition_value()

deferred_condition_typed()

deferred_then_error(fail)

deferred_then_panic()

deferred_then_warning_abort()

deferred_unit()

deferred_rng_value()

deferred_nested(f)

deferred_worker_value()

deferred_guard_altrep(mode)

deferred_guard_nested(panic_inner)

deferred_pending_count()

deferred_finalizer_pointer()

deferred_finalized_count()

deferred_guard_connection()
}
\arguments{
\item{n}{Row count.}

\item{fail}{Whether to return \code{Err}.}

\item{f}{An R function of no arguments.}

\item{mode}{Zero succeeds; 1 raises a classed error; 2 panics; 3 raises an R error; 4 uses the sum fallback.}

\item{panic_inner}{Whether the inner callback panics after queueing its warning.}
}
\description{
Returns \code{n - 2} and warns about the two dropped rows.

Three conditions in one call, two typed and one from the macro form, to check queue order and mixed kinds. Returns 3.

\code{defer_message} with a struct payload (Display message, renamed and Debug fields). Returns \code{"done"}.

\verb{defer_condition!} with a custom class: silent without a handler.

\code{defer_condition} with a typed payload on a plain condition.

A deferred warning followed by a classed \code{Err}: the warning is signalled first, then the error.

A deferred warning followed by \verb{rust_error!} (the panic transport).

A deferred warning followed by an immediate \verb{warning!} (which aborts the call): both arrive, deferred first, and the value is \code{NULL}.

Unit return: R gets \code{invisible(NULL)} plus the warning.

The \code{rng} arm of the main-thread template (Get/PutRNGstate around the body). Returns 1.

Nested calls: evaluates an R closure that itself calls \code{deferred_warning_value()}; the inner call flushes only its own warning, this call flushes only "outer".

Worker-thread template: the push happens on the worker, the signal on the main thread after the result is back. Returns 9.

Create an ALTREP whose element callback defers a warning and message.

Check that a low-level raising guard signals before the enclosing .Call.

Number of unsignalled entries, including entries below this .Call's mark.

An external pointer whose destructor attempts to queue a warning.

Number of completed deferred-warning finalizers.

Connection writes signal locally; close, destroy and Drop suppress queues.
}
