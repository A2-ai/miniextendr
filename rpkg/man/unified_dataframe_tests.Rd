% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/miniextendr-wrappers.R
\name{unified_roundtrip}
\alias{unified_roundtrip}
\alias{unified_roundtrip_count}
\alias{gc_stress_unified_dataframe}
\title{unified_roundtrip}
\source{
Generated by miniextendr from Rust fn \code{unified_roundtrip}

Generated by miniextendr from Rust fn \code{unified_roundtrip_count}

Generated by miniextendr from Rust fn \code{gc_stress_unified_dataframe}
}
\usage{
unified_roundtrip(df)

unified_roundtrip_count(df)

gc_stress_unified_dataframe()
}
\arguments{
\item{df}{A data.frame with numeric \code{x}, \code{y} and character \code{label} columns.}
}
\description{
Round-trip a \code{data.frame} through the unified verbs: read into \verb{Vec<Row>} with the \code{from_dataframe} reader, then rebuild with the \code{into_dataframe} writer.

Returns a fresh \code{data.frame} identical to the input, proving both public verbs compose. Takes a \code{SEXP} so it runs on the R main thread.

Read a \code{data.frame} into \verb{Vec<UnifiedPoint>} via the unified \code{from_dataframe} reader and return the row count.

GC-stress fixture for the unified DataFrame verbs.

No-arg so the fast \code{gctorture(TRUE)} sweep over \code{rpkg}'s exports exercises it. Builds rows, assembles a \code{DataFrame} with the \code{into_dataframe} writer (the SEXP-allocating write path), then reads them back with the \code{from_dataframe} reader while the assembled \code{DataFrame} SEXP is held live across the second allocation — the window where a GC barrier bug would surface.
}
