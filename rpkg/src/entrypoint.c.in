#include <R.h>
#include <Rinternals.h>
#include <R_ext/Rdynload.h>

extern void R_init_@rpkg@_miniextendr(DllInfo * dll);

void R_init_@rpkg@(DllInfo *dll) {
    R_init_@rpkg@_miniextendr(dll);
    
    R_useDynamicSymbols(dll, FALSE);
    R_forceSymbols(dll, TRUE);
}
