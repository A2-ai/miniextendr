/* Minimal stub.c — R's build system requires at least one C file to produce a shared library.
   miniextendr packages link the real Rust cdylib via the Makevars linker flags. */
void R_init_staticcheckrustpkg(void) {}
