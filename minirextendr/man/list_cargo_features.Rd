% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-detect-configure.R
\name{list_cargo_features}
\alias{list_cargo_features}
\title{List Cargo features and optional dependencies}
\usage{
list_cargo_features(path = ".")
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current directory.}
}
\value{
A list with:
\describe{
\item{features}{Named list: feature name -> character vector of specs
(e.g., \code{list(rayon = "miniextendr-api/rayon")}) }
\item{optional_deps}{Named list: dep name -> list with \code{version}
and \code{features} (character vector of enabled features)}
\item{without_rules}{Character vector of feature names that have no
corresponding detection rule in \code{tools/detect-features.R} (NULL if
detection is not set up)}
}
}
\description{
Runs \verb{cargo metadata} on \code{src/rust/Cargo.toml} and parses the output to
discover all defined features and optional dependencies. Useful when a crate
already has many features and you want to see which ones still need detection
rules.
}
\examples{
\dontrun{
info <- list_cargo_features()
info$features        # all features defined in [features]
info$optional_deps   # optional dependencies (auto-create features)
info$without_rules   # features missing detection rules
}
}
