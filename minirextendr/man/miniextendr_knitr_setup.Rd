% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knitr.R
\name{miniextendr_knitr_setup}
\alias{miniextendr_knitr_setup}
\title{Set up miniextendr knitr engine}
\usage{
miniextendr_knitr_setup(path = ".", ...)
}
\arguments{
\item{path}{Path to the R package root, \code{"."} to use the current directory,
or \code{NULL} for inline compilation mode.}

\item{...}{Additional arguments passed to \code{\link[=miniextendr_sync]{miniextendr_sync()}} (package mode)
or \code{\link[=rust_source]{rust_source()}} (inline mode).}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
Registers a custom knitr language engine named \code{"miniextendr"}.
}
\details{
When \code{path} is provided (default \code{"."}), runs \code{\link[=miniextendr_sync]{miniextendr_sync()}} to
ensure an existing package is built before knitting. Chunks are then
evaluated as R code.

When \code{path = NULL}, enables \strong{inline mode}: each \code{{miniextendr}} chunk
is compiled as standalone Rust code via \code{\link[=rust_source]{rust_source()}}, and compiled
functions are loaded into the knitr global environment. This is useful
for self-contained vignettes that don't require an existing package.
}
\examples{
\dontrun{
# Package mode (in a package vignette):
miniextendr_knitr_setup()

# Inline mode (self-contained vignette):
miniextendr_knitr_setup(path = NULL)
}
}
