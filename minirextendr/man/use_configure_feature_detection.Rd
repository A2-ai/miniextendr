% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-detect-configure.R
\name{use_configure_feature_detection}
\alias{use_configure_feature_detection}
\title{Set up configure-time feature detection}
\usage{
use_configure_feature_detection(path = ".")
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current directory.}
}
\value{
Invisibly returns TRUE
}
\description{
Creates the infrastructure for automatically detecting which Cargo features
to enable at \code{./configure} time. This is a one-time setup that:
}
\details{
\enumerate{
\item Creates \code{tools/detect-features.R} with empty rules
\item Patches \code{configure.ac} to call the script when the features env var isn't set
\item Runs \code{autoconf} to regenerate \code{configure}
}

After setup, use \code{\link[=add_feature_rule]{add_feature_rule()}} to add detection rules.
}
\examples{
\dontrun{
use_configure_feature_detection()
add_feature_rule("rayon", detect = TRUE, optional_dep = TRUE)
add_feature_rule("vctrs", detect = 'requireNamespace("vctrs", quietly = TRUE)')
}
}
