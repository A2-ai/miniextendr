% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{miniextendr_sync}
\alias{miniextendr_sync}
\title{Sync miniextendr package for rendering}
\usage{
miniextendr_sync(
  path = ".",
  mode = c("if_stale", "always", "never"),
  stage = c("install", "build"),
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current directory.}

\item{mode}{One of \code{"if_stale"} (default), \code{"always"}, or \code{"never"}.
Controls whether to rebuild:
\itemize{
\item \code{"if_stale"}: only rebuild when source hash has changed
\item \code{"always"}: rebuild unconditionally
\item \code{"never"}: skip rebuild entirely (useful for pre-built packages)
}}

\item{stage}{How far to rebuild. One of:
\itemize{
\item \code{"install"} (default): full rebuild (configure + install + roxygen2)
\item \code{"build"}: configure only, no install
}}

\item{force}{Logical. If \code{TRUE}, equivalent to \code{mode = "always"}.}

\item{quiet}{Logical. If \code{TRUE}, suppress progress messages.}
}
\value{
Invisibly returns a list with:
\itemize{
\item \code{fresh}: logical, whether sources were already up-to-date
\item \code{stage_run}: character, what stage was executed (or \code{"none"})
\item \code{hash}: character, current source hash
}
}
\description{
Detects staleness of Rust sources and build artifacts, then rebuilds
as needed. Designed as a single entry point before rendering vignettes
(Rmd/qmd). Dispatches to \code{\link[=miniextendr_build]{miniextendr_build()}}
and related workflow functions.
}
\examples{
\dontrun{
# Sync before rendering a vignette
miniextendr_sync()

# Force full rebuild
miniextendr_sync(force = TRUE)

# Configure only (no install)
miniextendr_sync(stage = "build")
}
}
