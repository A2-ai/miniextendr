% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cargo.R
\name{cargo_add}
\alias{cargo_add}
\title{Add a dependency to Cargo.toml}
\usage{
cargo_add(
  dep,
  features = NULL,
  no_default_features = FALSE,
  optional = FALSE,
  rename = NULL,
  path = NULL,
  git = NULL,
  branch = NULL,
  tag = NULL,
  rev = NULL,
  registry = NULL,
  dev = FALSE,
  build = FALSE,
  target = NULL,
  dry_run = FALSE,
  offline = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dep}{Dependency specification. Can be:
\itemize{
\item A crate name: \code{"serde"}
\item Name with version: \code{"serde@1.0"} or \code{"serde@=1.0.38"}
\item Multiple crates: \code{c("serde", "tokio@1.0")}
}}

\item{features}{Character vector of features to activate.}

\item{no_default_features}{Logical. If TRUE, disable default features.}

\item{optional}{Logical. If TRUE, mark dependency as optional (exposed as a crate feature).}

\item{rename}{Character. Rename the dependency (useful for multiple versions).}

\item{path}{Character. Path to local crate to add instead of from crates.io.}

\item{git}{Character. Git repository URL to add dependency from.}

\item{branch}{Character. Git branch (used with \code{git}).}

\item{tag}{Character. Git tag (used with \code{git}).}

\item{rev}{Character. Git revision/commit hash (used with \code{git}).}

\item{registry}{Character. Package registry name for this dependency.}

\item{dev}{Logical. If TRUE, add as dev-dependency (for tests/examples/benchmarks).}

\item{build}{Logical. If TRUE, add as build-dependency (for build.rs).}

\item{target}{Character. Add as dependency for specific target platform (e.g., "x86_64-unknown-linux-gnu").}

\item{dry_run}{Logical. If TRUE, show what would happen without modifying Cargo.toml.}

\item{offline}{Logical. If TRUE, run without network access.}

\item{quiet}{Logical. If TRUE, suppress cargo output.}
}
\value{
Invisibly returns TRUE on success
}
\description{
Wraps \verb{cargo add} to add Rust dependencies to your miniextendr package.
Automatically uses \code{src/rust/Cargo.toml} in the current R package.
}
\examples{
\dontrun{
# Add serde with derive feature
cargo_add("serde", features = "derive")

# Add specific version
cargo_add("tokio@1.0", features = c("full", "rt-multi-thread"))

# Add as dev dependency (for tests)
cargo_add("criterion", dev = TRUE)

# Add from git
cargo_add("mycrate", git = "https://github.com/user/mycrate")

# Add from git with specific branch
cargo_add("mycrate", git = "https://github.com/user/mycrate", branch = "dev")

# Add from local path
cargo_add("localcrate", path = "../my-local-crate")

# Add multiple crates at once
cargo_add(c("serde", "serde_json"))

# Add with no default features, only specific ones
cargo_add("tokio", no_default_features = TRUE, features = c("rt", "macros"))

# Add as optional dependency (becomes a crate feature)
cargo_add("rayon", optional = TRUE)

# Dry run to see what would happen
cargo_add("newcrate", dry_run = TRUE)
}
}
