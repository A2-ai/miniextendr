% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/git-lfs.R
\name{use_git_lfs}
\alias{use_git_lfs}
\title{Set up Git LFS tracking for vendor.tar.xz}
\usage{
use_git_lfs(path = ".", patterns = "inst/vendor.tar.xz")
}
\arguments{
\item{path}{Path to the R package (or monorepo) root.}

\item{patterns}{Character vector of glob patterns to track with LFS.
Defaults to \code{"inst/vendor.tar.xz"}. Set to \code{NULL} to only run
\verb{git lfs install} without adding patterns.}
}
\value{
Invisibly returns \code{TRUE} if changes were made, \code{FALSE} otherwise.
}
\description{
Configures Git LFS to track \code{inst/vendor.tar.xz} (the vendored Rust
dependencies archive). This prevents large binary files from bloating
the Git repository.
}
\details{
The function:
\enumerate{
\item Checks that \verb{git lfs} is available
\item Reads any existing \code{.gitattributes} (preserving user entries)
\item Adds \verb{inst/vendor.tar.xz filter=lfs diff=lfs merge=lfs -text} if not
already tracked
\item Runs \verb{git lfs install} if not already initialized
}
}
