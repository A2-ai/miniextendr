% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vendor.R
\name{vendor_miniextendr}
\alias{vendor_miniextendr}
\title{Download and vendor miniextendr crates}
\usage{
vendor_miniextendr(
  path = ".",
  version = "main",
  dest = NULL,
  refresh = FALSE,
  local_path = NULL
)
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current directory.}

\item{version}{Version tag to download (default: "main" for latest).
Ignored if \code{local_path} is provided.}

\item{dest}{Destination directory for vendored crates. Defaults to
\verb{vendor/} inside the project root.}

\item{refresh}{Force re-download even if cached (default: FALSE)}

\item{local_path}{Path to local miniextendr repository. If provided,
copies crates from local path instead of downloading from GitHub.}
}
\value{
Invisibly returns TRUE on success
}
\description{
Downloads miniextendr-api, miniextendr-macros,
miniextendr-lint, and miniextendr-engine from GitHub and vendors them
into vendor/. Also
patches Cargo.toml files to remove workspace inheritance.
}
\details{
Downloaded archives are cached in \code{tools::R_user_dir("minirextendr", "cache")}
to avoid repeated downloads of the same version.

For local development (when GitHub repo is not available), set
\code{local_path} to the path of the miniextendr repository.
}
