% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-detect-configure.R
\name{sync_feature_rules}
\alias{sync_feature_rules}
\title{Sync feature detection rules with Cargo.toml}
\usage{
sync_feature_rules(path = ".")
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current directory.}
}
\value{
Invisibly returns a character vector of newly added feature names
(empty if everything was already in sync).
}
\description{
Reads \verb{[features]} from \code{src/rust/Cargo.toml} via \verb{cargo metadata} and adds
detection rules for any features that don't have one yet. New features are
added with \code{detect = TRUE} (always-enable) by default. Run this after adding
new features to keep \code{tools/detect-features.R} up to date.
}
\details{
Skips the \code{"default"} pseudo-feature and any feature that already has a rule.
}
\examples{
\dontrun{
# After adding new Cargo features:
sync_feature_rules()
#> v Added feature detection rule for 'new_feature_1'
#> v Added feature detection rule for 'new_feature_2'
#> i 2 features added, 15 already had rules
}
}
