% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{miniextendr_check_static}
\alias{miniextendr_check_static}
\title{Run R CMD check without triggering a Rust compile}
\usage{
miniextendr_check_static(
  path = ".",
  args = c("--install=fake", "--no-manual"),
  build_args = c("--no-build-vignettes"),
  error_on = "warning",
  roxygenize = TRUE,
  ...
)
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current
directory.}

\item{args}{Character vector of arguments passed to \verb{R CMD check}.
Must contain \code{--install=fake}; a warning is emitted if a different
\verb{--install=} value is detected.}

\item{build_args}{Character vector of arguments passed to \verb{R CMD build}.}

\item{error_on}{Severity level at which to error. One of \code{"error"},
\code{"warning"}, or \code{"note"}. Passed to \code{\link[rcmdcheck:rcmdcheck]{rcmdcheck::rcmdcheck()}}.}

\item{roxygenize}{If \code{TRUE} (the default) and \code{roxygen2} is installed,
call \code{roxygen2::roxygenize(path)} before running the check. This
removes false-positive stale-documentation warnings without requiring
cargo. Silently skipped if \code{roxygen2} is not installed. Pass
\code{roxygenize = FALSE} in tests that intentionally exercise broken-Rd
code paths.}

\item{...}{Additional arguments forwarded to \code{\link[rcmdcheck:rcmdcheck]{rcmdcheck::rcmdcheck()}}.}
}
\value{
The \code{\link[rcmdcheck:rcmdcheck]{rcmdcheck::rcmdcheck()}} result object, invisibly.
}
\description{
Wraps \code{\link[rcmdcheck:rcmdcheck]{rcmdcheck::rcmdcheck()}} with \code{--install=fake} so that
\code{./configure} (and therefore \code{cargo}) is never invoked. This makes it
suitable for fast iteration on un-vendored packages, sandboxed
environments (e.g. Claude Code, CRAN incoming \code{--no-internet}), or any
context where \code{cargo} cannot reach the network.
}
\details{
All static phases still run. Phases that require a loaded package
are automatically skipped by R CMD check when \code{--install=fake} is active.

\strong{Catches:}
\itemize{
\item DESCRIPTION fields (Authors\@R, Version, License, dependency syntax)
\item NAMESPACE (export consistency, unresolved imports)
\item R code syntax and parse errors
\item Rd file parsing, cross-references, and metadata
\item code-usage analysis (\code{codetools} -- undefined globals, etc.)
\item sub-directory checks (\verb{man/}, \verb{tests/}, \verb{R/})
\item top-level file checks (LICENSE presence, etc.)
\item ASCII / encoding checks
}

\strong{Does not catch:}
\itemize{
\item Rust compile errors
\item \verb{#[miniextendr]} macro expansion errors
\item ABI / linkage issues
\item anything in \verb{tests/testthat/*} (tests are skipped)
\item examples in Rd files
\item vignettes
}

The \code{--install=fake} flag is honoured by R CMD check since at least R 3.5
(r-svn \code{tools/R/install.R:2424}, \code{tools/R/check.R:7373}).
}
\note{
Run \code{\link[=miniextendr_check]{miniextendr_check()}} for full validation (including Rust
compilation) before a CRAN submission.
}
\seealso{
\code{\link[=miniextendr_check]{miniextendr_check()}} for the full check including Rust
compilation.
}
