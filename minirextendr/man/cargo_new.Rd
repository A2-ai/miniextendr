% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cargo.R
\name{cargo_new}
\alias{cargo_new}
\title{Create a new Rust crate in a workspace}
\usage{
cargo_new(
  name,
  lib = TRUE,
  edition = "2024",
  vcs = "none",
  add_to_workspace = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{name}{Name of the crate to create. This will be the directory name.}

\item{lib}{Logical. If TRUE, create a library crate (default). If FALSE, create a binary.}

\item{edition}{Rust edition to use (default "2024").}

\item{vcs}{Version control system to initialize. One of "git", "hg", "pijul", "fossil", or "none".
Default is "none" to avoid nested git repos.}

\item{add_to_workspace}{Logical. If TRUE and in a workspace, add the new crate
to the workspace members list in Cargo.toml. Default is TRUE.}

\item{quiet}{Logical. If TRUE, suppress cargo output.}
}
\value{
Invisibly returns the path to the new crate directory
}
\description{
Wraps \verb{cargo new} to create a new Rust crate. Unlike \verb{cargo init}, this creates
a new directory for the crate. When in a Cargo workspace, runs from the workspace
root so the new crate is workspace-aware.
}
\details{
Note: \verb{cargo new} does not accept \code{--manifest-path}, so this function changes
to the appropriate directory before running the command.
}
\examples{
\dontrun{
# Create a new library crate
cargo_new("my-utils")

# Create a binary crate
cargo_new("my-cli", lib = FALSE)

# Create without adding to workspace
cargo_new("standalone-crate", add_to_workspace = FALSE)
}
}
