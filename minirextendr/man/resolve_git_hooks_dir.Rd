% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/git-hooks.R
\name{resolve_git_hooks_dir}
\alias{resolve_git_hooks_dir}
\title{Resolve the git hooks directory for a package directory}
\usage{
resolve_git_hooks_dir(proj_dir)
}
\arguments{
\item{proj_dir}{Package directory to resolve from.}
}
\value{
Absolute path to the hooks directory, or \code{NULL} when git is
unavailable or \code{proj_dir} is not inside a git repository.
}
\description{
Asks git itself where hooks live, from the perspective of \code{proj_dir}:
\verb{git rev-parse --git-path hooks}. Going through git (rather than assuming
\verb{<proj>/.git/hooks}) means the result is correct when
}
\details{
\itemize{
\item the package is nested inside a larger repo (the enclosing repo's hooks dir
is returned, not a non-existent \verb{<proj>/.git});
\item \code{proj_dir} is a linked git worktree, whose \code{.git} is a \emph{file} (a gitlink)
rather than a directory — git returns the common hooks dir;
\item \code{core.hooksPath} is configured (git >= 2.36 reflects it here — verified
against this repo's own \code{core.hooksPath = .githooks}).
}

\code{--git-path} yields a path relative to the current directory; \code{run_command()}
runs git with the working directory set to \code{proj_dir}, so a relative result
is anchored there before being normalised to an absolute path.
}
\keyword{internal}
