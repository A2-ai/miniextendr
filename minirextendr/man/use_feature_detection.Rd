% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use-feature.R
\name{use_feature_detection}
\alias{use_feature_detection}
\title{Generate feature detection code}
\usage{
use_feature_detection(
  package_name = NULL,
  features = NULL,
  rust_file = "src/rust/feature_detection.rs",
  r_file = "R/feature_helpers.R",
  overwrite = FALSE
)
}
\arguments{
\item{package_name}{Package name (default: derived from DESCRIPTION)}

\item{features}{Character vector of feature names to include. Default NULL
scans Cargo.toml.in automatically.}

\item{rust_file}{Path for generated Rust code (default: "src/rust/feature_detection.rs")}

\item{r_file}{Path for generated R helpers (default: "R/feature_helpers.R")}

\item{overwrite}{Logical, whether to overwrite existing files}
}
\value{
Invisibly returns list of generated file paths
}
\description{
Generates Rust code that exposes enabled Cargo features to R, and R helper
functions to query them. This allows tests to skip when features are missing.
}
\details{
This function scans your \code{src/rust/Cargo.toml.in} for features and generates:
\enumerate{
\item \strong{Rust code} (\verb{<package>_enabled_features()}) - Returns a vector of enabled feature names
\item \strong{R helpers} (\code{has_feature()}, \code{skip_if_missing_feature()}) - For runtime feature checks
}

The generated Rust function uses \verb{cfg!(feature = "...")} at compile time to build
a list of enabled features. This is useful for:
\itemize{
\item Skipping tests when optional features are not compiled in
\item Conditional code paths based on available features
\item Documentation of what's included in a build
}
}
\examples{
\dontrun{
# Auto-detect features from Cargo.toml.in
use_feature_detection()

# Manually specify features
use_feature_detection(features = c("rayon", "serde", "uuid"))
}
}
