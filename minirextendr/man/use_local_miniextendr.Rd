% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local-miniextendr.R
\name{use_local_miniextendr}
\alias{use_local_miniextendr}
\title{Wire a local miniextendr framework checkout (dev-only)}
\usage{
use_local_miniextendr(miniextendr_path, path = ".")
}
\arguments{
\item{miniextendr_path}{Path to a local miniextendr repository checkout.
Canonicalized to an absolute POSIX path; the directory must contain
\code{miniextendr-api/Cargo.toml}.}

\item{path}{Path to the R package root, or \code{"."} to use the current
directory (house convention, matching e.g. \code{use_vendor_lib()}).}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
Records an absolute path to a local miniextendr checkout in a one-line
\code{.miniextendr-local} marker file at the R package root.  When
\verb{bash ./configure} runs, it reads this file and sets \code{MONOREPO_ROOT}
to the recorded path so the \verb{[patch."https://github.com/A2-ai/miniextendr"]}
block in \code{.cargo/config.toml} resolves the framework crates from the local
tree.  Tarball mode (\code{inst/vendor.tar.xz} present) always takes precedence.
}
\details{
The marker file is added to \code{.gitignore} and \code{.Rbuildignore} automatically,
so it can never appear in a distributed tarball.
}
\seealso{
\code{\link[=unuse_local_miniextendr]{unuse_local_miniextendr()}}
}
