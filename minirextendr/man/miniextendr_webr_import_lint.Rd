% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/webr-lint.R
\name{miniextendr_webr_import_lint}
\alias{miniextendr_webr_import_lint}
\title{Lint namespace imports for webR compatibility}
\usage{
miniextendr_webr_import_lint(path = ".", lib_paths = .libPaths())
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current
directory.}

\item{lib_paths}{Library paths to probe for installed dependencies.
Defaults to \code{.libPaths()}.}
}
\value{
Invisibly, a data frame with columns \code{package}, \code{directive},
\code{status} (one of \code{"compiled"}, \code{"known-compiled"}, \code{"unknown"},
\code{"pure-r"}), and \code{via} (the compiled or unverifiable packages reached
through the namespace-load graph, comma-joined).
}
\description{
Flags namespace-level \code{importFrom()} / \code{import()} (and
\code{importClassesFrom()} / \code{importMethodsFrom()}) directives whose target
package -- or anything in its hard (\code{Depends}/\code{Imports}) dependency
graph -- ships compiled code. Under webR, the wasm \verb{R CMD INSTALL}
lazy-load step spawns a \emph{host} (native) R whose \code{.libPaths()} points into
the wasm library tree; namespace-loading a compiled dependency there makes
the host R \code{dyn.load()} a wasm-built \code{.so} and the install fails with
\verb{invalid ELF header}. The fix is to demote compiled dependencies to
\code{Suggests} and call them via \code{pkg::fn()} behind \code{requireNamespace()}.
}
\details{
Detection is a local static probe -- no \code{loadNamespace()} (that is the
very failure being guarded against) and no network. An installed
dependency counts as compiled if it has a \verb{libs/} directory or
\code{NeedsCompilation: yes} in its \code{DESCRIPTION}; pure-R dependencies are
walked recursively so pure-R umbrella packages over compiled hard imports
(e.g. shiny over httpuv) are still caught. Dependencies that are not
installed locally fall back to a curated list of well-known compiled
packages; anything not on that list is reported as unverifiable, not
flagged.

Base-priority packages (\code{stats}, \code{utils}, \code{methods}, ...) are exempt: the
host R resolves them from its own \code{R.home("library")}, never from the wasm
library tree.
}
\seealso{
\code{\link[=miniextendr_doctor]{miniextendr_doctor()}} with \code{webr = TRUE} runs this lint as part
of the full diagnostic.
}
