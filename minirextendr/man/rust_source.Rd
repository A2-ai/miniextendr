% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rust-source.R
\name{rust_source}
\alias{rust_source}
\title{Compile inline Rust code and load functions into R}
\usage{
rust_source(
  file = NULL,
  code = NULL,
  env = parent.frame(),
  cache = TRUE,
  quiet = FALSE,
  features = character(),
  use_local_crates = NULL
)
}
\arguments{
\item{file}{Path to a \code{.rs} file containing Rust code with \verb{#[miniextendr]}
functions and a \verb{miniextendr_module!} block. Mutually exclusive with \code{code}.}

\item{code}{Character string of inline Rust code. Mutually exclusive with \code{file}.}

\item{env}{Environment to load compiled functions into (default: caller's environment).}

\item{cache}{Logical. If \code{TRUE} (default), reuses previously compiled packages
when the code and features are unchanged.}

\item{quiet}{Logical. If \code{TRUE}, suppresses build output.}

\item{features}{Character vector of cargo features to enable.}

\item{use_local_crates}{Path to a local miniextendr repository for vendoring.
If \code{NULL} (default), auto-detects via \code{MINIEXTENDR_LOCAL} env var or
parent directory scan.}
}
\value{
Invisibly returns a list with components:
\itemize{
\item \code{functions}: character vector of exported function names
\item \code{dll}: path to the compiled shared library
\item \code{package}: package name
\item \code{cached}: logical, whether the result was served from cache
}
}
\description{
Takes a \code{.rs} file or inline code string, scaffolds a temporary miniextendr
R package, builds it, and loads the compiled functions into the specified
environment. Similar to Rcpp's \code{sourceCpp()}.
}
\examples{
\dontrun{
# Compile inline Rust code
rust_source(code = '
use miniextendr_api::{miniextendr, miniextendr_module};

#[miniextendr]
pub fn add_one(x: i32) -> i32 { x + 1 }

miniextendr_module! {
    mod placeholder;
    fn add_one;
}
')
add_one(41L)
#> [1] 42
}
}
