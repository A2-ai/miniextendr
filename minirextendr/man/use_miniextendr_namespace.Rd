% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use-r.R
\name{use_miniextendr_namespace}
\alias{use_miniextendr_namespace}
\title{Ensure a minimal roxygen2-compatible NAMESPACE exists}
\usage{
use_miniextendr_namespace(path = ".")
}
\arguments{
\item{path}{Path to the R package root, or \code{"."} to use the current directory.}
}
\value{
Invisibly returns TRUE if a NAMESPACE was created, FALSE if one
already existed.
}
\description{
\code{usethis::create_package()} normally seeds a \code{NAMESPACE}, but
\code{use_miniextendr()} may be run against a package that was set up some other
way (e.g. via \code{usethis::use_description()} in an existing repo). Without a
\code{NAMESPACE} the first build fails on the configure-time guard. This seeds a
minimal \code{NAMESPACE} carrying the roxygen2 header plus the
\verb{useDynLib(<pkg>, .registration = TRUE)} directive, so the shared library
loads and a later \code{devtools::document()} manages (rather than clobbers) the
file. No-op when a \code{NAMESPACE} already exists.
}
\keyword{internal}
