docs/
.cache
__pycache__

# Generated by generate-miniextendr-docs.sh (`just llm-docs`) from rustdoc JSON:
# the LLM-ready API digests and trait-impl inventories of every root-workspace
# crate plus cargo-revendor. Untracked (like rpkg's wrappers.R and
# wasm_registry.rs) because every public-API change rewrote them, so almost
# every open PR conflicted on them. Regenerate on demand with `just llm-docs`.
generated/
