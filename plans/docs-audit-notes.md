# docs audit notes — optionals + wasm_registry

Sourced from audit alongside PR #675 (ffi → sys split). Three insertion points.

## Insertion 1: `miniextendr-api/src/optionals.rs` line 37 (after feature table)

Add a `//! # Module template` section documenting the standard regional layout
(scalar, option, vector, Vec<Option<T>>, R*Ops adapter, helpers, tests) plus
named exceptions (rayon_bridge, serde_impl, array-heavy upstreams).

Placement: between the last `//! |` table row (line 37) and the first
`// region:` marker (line 38).

## Insertion 2: `CLAUDE.md` — after "How the build works" section, before "## R version (rv + rig)"

Add `## Generated artifacts (do not hand-edit, do not count toward LoC)` with
one bullet for `rpkg/src/rust/wasm_registry.rs` (~11.8K lines, wasm32-target
substitute for linkme tables, generated by `miniextendr_write_wasm_registry`).

Second bullet (`rpkg/R/miniextendr-wrappers.R` + NAMESPACE + man/) is already
covered at line 224; drop to avoid duplication.

## Insertion 3: `AGENTS.md` — after "### `inst/vendor.tar.xz` is not tracked", before "## Key Concepts"

Mirror the same `## Generated artifacts` section from Change 2.

## Commits

1. `plan: docs audit notes for optionals + wasm_registry`
2. `docs: add optionals module template + generated-artifacts note`
